<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Personal Website" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Personal Website">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css">
    <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.js"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/contrib/auto-render.min.js"
      onload="renderMathInElement(document.body);"></script>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
  <nav data-controller="navbar" class="bg-white text-gray-800 p-4 border-b border-gray-200">
  <div class="container mx-auto flex flex-wrap justify-between items-center">
    
    <%= link_to "Jimmy Lin", root_path, class: "text-xl font-bold font-mono tracking-wider hover:text-blue-600 transition" %>

    <button data-action="click->navbar#toggle" class="md:hidden focus:outline-none text-gray-600 hover:text-black">
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
      </svg>
    </button>

    <div class="hidden md:flex items-center space-x-6">
      <%= link_to "Home", root_path, class: "hover:text-blue-600 font-medium transition" %>
      <%= link_to "Blog", "/posts", class: "hover:text-blue-600 font-medium transition" %>
      <%= link_to "Projects", "/projects", class: "hover:text-blue-600 font-medium transition" %>
      <%= link_to "About", "/about", class: "hover:text-blue-600 font-medium transition" %>
     

      <%= form_with url: "/posts", method: :get, class: "relative" do |form| %>
        <%= form.text_field :query, placeholder: "Search...", class: "border border-gray-300 rounded-full py-1 px-3 text-sm focus:outline-none focus:border-blue-500" %>
      <% end %> <% if authenticated? %>
        <%= link_to "Admin", "/admin", class: "text-red-600 hover:text-red-800 font-medium" %>
        <%= button_to "Log out", "/logout", method: :delete, class: "bg-gray-200 hover:bg-gray-300 px-3 py-1 rounded text-sm transition" %>
      <% end %> </div>
  </div>

  <div data-navbar-target="mobileMenu" class="hidden md:hidden w-full mt-4 border-t border-gray-100 pt-4">
    <div class="flex flex-col space-y-3">
      <%= link_to "Home", root_path, class: "block hover:bg-gray-50 px-3 py-2 rounded" %>
      <%= link_to "Blog", "/posts", class: "block hover:bg-gray-50 px-3 py-2 rounded" %>
      <%= link_to "Projects", "/projects", class: "block hover:bg-gray-50 px-3 py-2 rounded" %>
      <%= link_to "About", "/about", class: "block hover:bg-gray-50 px-3 py-2 rounded" %>
      
      <% if authenticated? %>
        <%= link_to "Admin Panel", "/admin", class: "block text-red-600 hover:bg-gray-50 px-3 py-2 rounded" %>
        <%= button_to "Log out", "/logout", method: :delete, class: "w-full text-left hover:bg-gray-50 px-3 py-2 rounded" %>
      <% end %> <div class="px-3 pt-2">
        <%= form_with url: "/posts", method: :get do |form| %>
          <%= form.text_field :query, placeholder: "Search posts...", class: "w-full border border-gray-300 rounded p-2 text-sm" %>
        <% end %> </div>
    </div>
  </div>
</nav>

  <% flash.each do |key, value| %>
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4">
      <div class="p-4 rounded-md <%= key == 'alert' ? 'bg-red-50 text-red-700' : 'bg-green-50 text-green-700' %>">
        <%= value %>
      </div>
    </div>
  <% end %>

  <main>
    <%= yield %>
  </main>
</body>
</html>
